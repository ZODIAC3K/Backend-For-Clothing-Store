const authController = require('./authController');
const imageController = require('./imageController')
const emailVarification = require('./emailVarification')

module.exports = {
    authController,
    imageController,
    emailVarification
}